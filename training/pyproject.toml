[project]
name = "training"
version = "0.1.0"
description = "Offline DPO micro-anneal for grammar-guided-preference-data (see ../docs/architecture.md)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
